spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mysql://localhost:3306/spring_study?useSSL=false
    username: jden
    password: jden
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
